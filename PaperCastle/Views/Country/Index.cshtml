@using PaperCastle.Application.Dto
@model List<CountryDto>

@{
    ViewData["Title"] = "Countries";
}

<div class="container">
    <h2>Genre List</h2>
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>Country Name</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var country in Model)
            {
                <tr>
                    <td><a asp-action="Details" asp-route-id="@country.Id">@country.Name</a></td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@country.Id"class="btn btn-primary">Edit</a>
                        <button class="btn btn-danger" data-toggle="modal" data-target="#deleteModal-@country.Id">Delete</button>
                    </td>
                </tr>
                <div class="modal fade" id="deleteModal-@country.Id" tabindex="-1" role="dialog" aria-labelledby="deleteModalLabel" aria-hidden="true">
                    <div class="modal-dialog" role="document">
                        <div class="modal-content">
                            <div class="modal-header">
                                <h5 class="modal-title" id="deleteModalLabel">Delete Country</h5>
                                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                                    <span aria-hidden="true">&times;</span>
                                </button>
                            </div>
                            <div class="modal-body">
                                Are you sure you want to delete this country?
                            </div>
                            <div class="modal-footer">
                                <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancel</button>
                                <a asp-action="Delete" asp-route-countryId="@country.Id" class="btn btn-danger">Delete</a>
                            </div>
                        </div>
                    </div>
                </div>
            }
        </tbody>
    </table>
</div>

@await Html.PartialAsync("_CreateItem", "Country")

<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
<script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>
